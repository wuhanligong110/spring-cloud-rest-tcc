// 项目启动文件
angular.module('starter', ['ionic','route','global','config','ionicLazyLoad','indexedDB','commonJs'])
  .run(function($ionicPlatform,$location,$ionicHistory,$rootScope) {
    $ionicPlatform.ready(function() {

    });
});

